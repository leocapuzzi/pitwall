[pytest]
# So coleta a suite nova em tests/. Os src/test_*.py antigos sao SCRIPTS (rodam no
# import, com caminho absoluto de uma maquina e login ao vivo do iRacing) — ficam de fora.
testpaths = tests
